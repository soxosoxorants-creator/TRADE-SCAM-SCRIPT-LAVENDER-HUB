-- [[ TRUE LAVENDER COMPACT UI ]]
local Library = loadstring(game:HttpGet("https://raw.githubusercontent.com/bloodball/-back-ups-for-libs/main/wallop"))()

-- Set Dimensions to match the small yellow menu (Small & Vertical)
local Window = Library:CreateWindow({
    Name = "Lavender Hub V4",
    Title = "Lavender Hub V4",
    Size = UDim2.new(0, 260, 0, 320), -- Forced Small Dimensions
    Color = Color3.fromRGB(180, 160, 255), -- True Lavender
})

-- ONLY THE TRADE TAB (No Farm, No Bloat)
local Main = Window:CreateTab("Main")

-- MATCHING THE PICTURE EXACTLY
Main:CreateToggle({
    Name = "Freeze Trade",
    Default = false,
    Callback = function(v) 
        -- Silent
    end
})

Main:CreateToggle({
    Name = "Auto Accept",
    Default = false,
    Callback = function(v) 
        -- Silent
    end
})

Main:CreateToggle({
    Name = "Auto Add Items",
    Default = false,
    Callback = function(v) 
        -- Silent
    end
})

-- AUTHENTIC LABELS
Main:CreateLabel("Status: Protected")
Main:CreateLabel("Version: 4.0.2 [Stable]")

-- NO AUTO FARM CODE EXISTS BELOW THIS LINE
