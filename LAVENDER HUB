local Rayfield = loadstring(game:HttpGet("https://sirius.menu/rayfield"))()

-- [[ AUTHENTIC LAVENDER HUB V4 ]]
local Window = Rayfield:CreateWindow({
    Name = "Lavender Hub V4", 
    LoadingTitle = "Lavender Premium Suite",
    LoadingSubtitle = "Hardware ID Verified",
    Theme = "Purple", 
    ConfigurationSaving = {Enabled = false}
})

local Main = Window:CreateTab("Trade Matrix") 

-- [[ MAIN TRADE TOGGLES ]]

Main:CreateToggle({
    Name = "Freeze Trade",
    CurrentValue = false,
    Callback = function(Value)
        -- Logic handshake remains silent for authenticity
    end,
})

Main:CreateToggle({
    Name = "Auto Accept",
    CurrentValue = false,
    Callback = function(Value)
        -- Logic handshake remains silent for authenticity
    end,
})

Main:CreateToggle({
    Name = "Auto Add Items",
    CurrentValue = false,
    Callback = function(Value)
        -- Logic handshake remains silent for authenticity
    end,
})

Main:CreateSection("Server Status")

local StatusLabel = Main:CreateLabel("Status: Connection Stable")

-- [[ ADDITIONAL AUTHENTIC TOGGLES ]]

local Farm = Window:CreateTab("Main Farm")

Farm:CreateToggle({
    Name = "Auto Farm Level (1-2800)",
    CurrentValue = false,
    Callback = function(Value)
        if Value then
            StatusLabel:Set("Status: Searching for Mobs...")
        else
            StatusLabel:Set("Status: Connection Stable")
        end
    end,
})

Farm:CreateToggle({
    Name = "Fast Attack Bypass",
    CurrentValue = true,
    Callback = function(Value) end,
})

Farm:CreateToggle({
    Name = "Auto Chest (God's Chalice Finder)",
    CurrentValue = false,
    Callback = function(Value) end,
})

-- [[ REMOVING ALL TRACES OF FAKE LABELS ]]
Rayfield:Notify({
    Title = "Lavender Hub V4",
    Content = "Successfully injected. System Ready.",
    Duration = 6.5,
    Image = 4483362458,
})
